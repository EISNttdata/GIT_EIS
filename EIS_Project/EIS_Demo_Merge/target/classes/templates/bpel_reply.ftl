<!-- Generate reply to synchronous request -->
    <reply name="${reply.name}" partnerLink="${reply.partnerLink}" portType="${reply.portType}"
           operation="${reply.operation}" variable="${reply.variable}"/>